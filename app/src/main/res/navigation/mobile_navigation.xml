<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="tk.pokatomnik.suspicious.ui.home.HomeFragment"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/nav_add_password"
        android:name="tk.pokatomnik.suspicious.ui.newpassword.NewPasswordFragment"
        android:label="@string/menu_new_password"
        tools:layout="@layout/fragment_password_form" />

    <fragment
        android:id="@+id/nav_edit_password"
        android:name="tk.pokatomnik.suspicious.ui.editpassword.EditPasswordFragment"
        android:label="@string/menu_edit_password"
        tools:layout="@layout/fragment_password_form" />
</navigation>